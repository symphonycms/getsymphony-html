<?xml version="1.0" encoding="utf-8" ?>
<data>
    <learn-concept-view>
        <section id="58" handle="concepts">Concepts</section>
        <entry id="33213" tutorials="0" tutorial-steps="0" articles="0" concepts="0" tasks="0" resources="0" issues="0">
            <audiences>
                <item handle="beginners">Beginners</item>
                <item handle="users">Users</item>
                <item handle="developers">Developers</item>
            </audiences>
            <description mode="formatted"><h4>Overview</h4>
<p>System IDs are unique, auto-incrementing, numerical values that are autogenerated for all content <a rel="concept">entries</a> in a Symphony project. An entry’s system ID behaves like a hidden system field—invisible during entry creation and editing, but available to <a rel="concept">data sources</a> for <a rel="concept" href="data-source-filters">filtering</a>, sorting, and output.</p>
<h4>Usage</h4>
<p>System IDs are auto-generated upon entry creation and are not editable.</p>
<p>The system ID field is available to data sources for filtering, sorting, and populating <a rel="concept" href="data-source-output-parameters">output parameters</a>.</p>
<h4>Details</h4>
<p>Whenever an entry is returned by a data source, its System ID is provided as an attribute in its <a rel="concept">XML</a>.</p>
<pre><code>
    &lt;entry id="1"&gt;
        &lt;!-- Entry fields XML --&gt;
    &lt;/entry&gt;</code></pre>
<h4>The Big Picture</h4>
<p><a rel="concept">Field types</a> that create relationships among entries, such as <a rel="concept" href="field-types#select-box-link">Select Box Link</a>, use system IDs to manage those associations. A section called &#8220;Children,&#8221; for instance, might use a Select Box Link field called &#8220;Parent&#8221; to point to a section called &#8220;Parents.&#8221; The field would store the parent&#8217;s system ID, this way any of the parent entry&#8217;s content could change and the link would still be maintained.</p></description>
            <name handle="system-ids">System IDs</name>
            <parents>
                <item id="33147" handle="entries" section-handle="concepts" section-name="Concepts">Entries</item>
            </parents>
            <summary mode="formatted"><p>System IDs provide unique identification numbers for a Symphony project&#8217;s content entries.</p></summary>
        </entry>
    </learn-concept-view>
</data>